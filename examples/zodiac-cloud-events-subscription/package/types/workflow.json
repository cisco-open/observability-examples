{
  "name": "workflow",
  "allowedLayers": ["TENANT"],
  "idGeneration": {
    "generateRandomId": true
  },
  "jsonSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "workflow Object",
    "description": "workflow Object",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "name": {
          "type": "string",
          "description": "The workflow name."
      },
      "type": {
        "type": "string",
        "description": "A workaround for Zodiac CE GW, should be 'workflow'."
      },
      "workflow": {
        "type": "object",
        "description": "The workflow object."
      }
    },
    "required": [
      "name",
      "workflow",
      "type"
    ]
  }
}